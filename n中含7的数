#include<stdio.h>
int ok(int n)
{
	while(n)
{
	if(n%10==7)
	{
		return 1;
	}
	n/=10;}
	return 0;
}
int main()
{
	int i,j;
	scanf("%d",&j);
	for(i=1;i<=j;i++)
	{
		if(ok(i))
		printf("%d ",i)	;
	}

	return 0;
}
